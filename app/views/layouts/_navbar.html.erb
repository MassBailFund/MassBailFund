<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>"><%= t('title') %></a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if can? :new, Client %>
          <li><%= link_to t('navbar.new_client'), new_client_path %></li>
        <% end %>
        <% if can? :index, Client %>
          <li><%= link_to t('navbar.index_client_scope_open'), admin_clients_path(scope: 'open') %></li>
        <% end %>
        <% if can? :index, Client %>
          <li><%= link_to t('navbar.index_client'), admin_clients_path %></li>
        <% end %>
        <% if can? :index, :reports %>
          <li><%= link_to t('navbar.index_reports'), admin_reports_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
